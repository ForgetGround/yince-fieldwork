# 银策编译器 · 展业版

面向小微客户经理的可解释展业工作台。可交互参赛原型，使用 20 位模拟客户。

## 启动与验证

```sh
npm install
npm run dev
node --test tests/workbench.test.ts
npx tsc --noEmit
npm run build
```

Node.js 24+；React 19、TypeScript、Vinext、Tailwind 和 Radix / Shadcn。逻辑集中在 `lib/workbench.ts`，界面位于 `app/page.tsx` 与 `components/workbench-views.tsx`。

## 五分钟演示

1. 从侧栏打开“政策规则库”，点击“原文依据”。查看截图来源与字段表达式，或在“附件产品库”查看 ET 工作表单元格出处。
2. 处理经营年限从 1 年调整为 2 年的模拟通知。勾选人工确认后生效，旧规则归档。KH-004 等经营 1.5 年的客户核查随之变化。
3. 打开“客户机会”。查看 20 位客户排序，搜索编号 / 行业或按机会类型筛选。支持 JSON 导入、字段白名单、重复编号校验、未知值保留与合并。
4. 打开 KH-001 的访前作战单，查看 45 天到期、采购计划等证据，核对规则与未知信息。确认建议后进入拜访记录。
5. 填入模拟拜访记录，生成待确认纪要，人工核对及编辑后保存，自动创建跟进任务。可导出 CRM JSON，并查看操作审计及历史规则快照。

## 附件处理

- `P020250723600413272167.et` 是可读取的 WPS / BIFF 工作簿，Sheet1 有五款信贷产品。只提取 C:L 业务列并保留行号；联系人和联系方式没有复制到项目。
- 表内五款产品是税易贷、政采贷、智享贷、科企融、九融贷。原文保存在 `data/products.json`，不默认认为其利率、额度或制度版本当前有效。
- 截图 3 产品名称、生效日期、完整额度 / 期限信息缺失。称作“经营流水类贷款（产品名称待核实）”。8 条初始演示规则逐条引用原文位置。
- 企业经营年限与核心人员从业经验分别处理；稳定经营作为独立条件。对“原则上”、原截图缺第（7）项和其他条件保留人工复核。
- 截图 2 是 2023 年九派贷 / 九派通新闻，与表内九融贷分开，不合并其准入规则。
- “经营年限改为两年”明确标记为模拟通知。

## 能力与边界

这是本机业务规则与模板演示，没有调用大模型或连接银行生产系统。客户 / 规则 / 任务 / 纪要 / 操作记录保存在当前浏览器 localStorage，刷新后保留；不同设备不共享。固定业务基准日为 2026-09-15，实际审计操作记录当前时间。

- 客户优先级是演示权重之和，上限 100。未知特征不加分；得分不表示授信结论或审批概率。
- 条件核查采用符合 / 暂未满足 / 待核实三态，产品规则变化不把客户自动拒贷。
- TXT / Markdown 候选规则编译器仅处理明确“满 / 不少于 / 至少”的简单经营年限条件，拒绝歧义上限、严格不等式、复合例外。其他段落保留人工整理，不宣称已全面解析。
- Word、PDF、ET 的通用上传解析尚未接入，需先提取成文本；给定 ET 附件已完成离线提取并内置。
- 拜访纪要使用可审查的原文句子提取，未提及内容留空；人工可编辑并确认。没有自动写入 CRM。
- 日志和角色均为演示，不具备真实身份认证、服务端防篡改、持久数据库、角色权限或生产合规能力。私有站点访问控制由 Sites 提供。
- 正式接入需要文档解析 / OCR / LLM 结构化提取、经授权脱敏数据、规则审核工作流、服务端版本与审计存储、身份权限和银行 CRM 接口。

## Agent 工具

支持 `document.modelContext` 时注册 `list_customer_opportunities`（只读）与 `open_customer_brief`（打开并记录查看）。工具不确认规则或建议、不执行审批。无 WebMCP 的浏览器仍可操作完整界面。
