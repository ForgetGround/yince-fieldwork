# 2026-09-15 部署验证记录

## 发布内容

- 公网入口：<https://yince.xcoria.cn/>，HTTP 自动跳转 HTTPS。
- 公开源码：<https://github.com/ForgetGround/yince-fieldwork>。
- 静态发布目录：`/srv/yince/releases/20260915-4c8ba70`。
- 本次采用 Next.js 静态导出；没有部署业务数据库或大模型服务。

## 已通过检查

- 6 项业务逻辑测试、TypeScript 检查和 VPS 静态构建通过。
- 线上 HTML 与首页引用的资源共 10 个文件，与本地构建内容的 SHA-256 一致。
- HTTPS 首页返回 200，HTTP 返回 301，`/healthz` 返回 `yince-ok`。
- `.env`、`.git/config`、其他项目 API 路径和未知页面均返回 404。
- 浏览器能打开 KH-001 作战单，展示证据、三态核查和人工确认入口；未确认时“记录拜访”禁用。浏览器未捕获 warning / error 日志。
- 独立服务只监听 `127.0.0.1:59603`，启动时内存约 1.8 MB，限制 96 MB；启用开机启动。
- 在银策服务的实际文件系统命名空间内以运行账号检查：自身发布文件、配置不可写；其他项目数据目录、代码、主 Nginx 配置和证书目录不可读。
- 正式服务已启用 `NoNewPrivs` 与 seccomp，过滤 `connect` 系统调用。临时测试单元采用相同过滤参数，主动连接回环端口收到预期 `EPERM`；静态服务入站访问正常。
- 其他项目的 Nginx 配置与订单服务单元共 5 个文件摘要保持一致，原官网页面摘要保持一致。
- 入口 Nginx、订单服务与两项网络服务均维持原主进程，重启计数未增加；原官网、订单页面和公开产品目录 API 返回 200。
- Git 历史的 105 个独立文件对象已检查常见凭据格式，未发现匹配项；仓库未包含 SSH / GitHub 凭据或原始客户资料。

## HTTPS 与续期

独立证书已签发，当前有效期至 **2026-12-14**。`certbot.timer` 已启用，专用续期钩子只处理本域名，钩子校验和 Nginx 平滑重载已成功执行。

两次 `certbot renew --cert-name yince.xcoria.cn --dry-run` 均遇到 Let's Encrypt **测试环境**的 `rateLimited / Service busy; retry later`。因此本记录不声称自动续期端到端演练已通过；正式证书签发与 HTTPS 服务正常。测试服务恢复后，可重新执行上述命令，不应反复申请正式证书。

## 范围

以上是部署、资源与进程隔离及原型功能验证，不代表银行生产合规认证、渗透测试或多租户安全保证。主机内核和公网入口仍共享；应用数据仍在访问者浏览器中保存。
